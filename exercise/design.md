# Reuse decision

TODO: Compare an AVM candidate with this small network baseline contract.

The network baseline owns a VNet and named subnets in an existing RG.
The subnet-security boundary will receive subnet IDs in Lab 3.
Explain why provider configuration, credentials and backend stay outside modules.
