# TODO: create azurerm_virtual_network.this using the caller's existing RG,
# location, address_space and tags. Add standalone azurerm_subnet.this using
# for_each = var.subnets. Disable subnet default outbound access.
# AgentAlvine gives the current task in the exercise issue.
# Lab 3 covers subnet-security; do not add a backend/provider or create an RG.
